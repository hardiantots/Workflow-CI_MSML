## Mengakses melalui docker

Anda dapat menarik (pull) dan menjalankan Docker image ini dengan perintah berikut:

```bash
docker pull hardiantots/bank_model:latest
docker run -p 8080:8080 hardiantots/bank_model
